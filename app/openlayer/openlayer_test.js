'use strict';

describe('myApp.openlayer module', function() {

  beforeEach(module('myApp.openlayer'));

  describe('openlayer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var openlayerViewCtrl = $controller('OpenLayerViewCtrl');
      expect(openlayerViewCtrl).toBeDefined();
    }));

  });
});