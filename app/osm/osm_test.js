'use strict';

describe('myApp.osm module', function() {

  beforeEach(module('myApp.osm'));

  describe('osm controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var osmViewCtrl = $controller('OSMViewCtrl');
      expect(osmViewCtrl).toBeDefined();
    }));

  });
});